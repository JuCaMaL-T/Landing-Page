---
import "../styles/global.css";
import Header from "../components/layouts/Header.tsx";
import Footer from "../components/layouts/Footer.tsx";
const { children, mesh = false } = Astro.props;
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>Jucamal Tecnología</title>
	</head>
	<body class="bg-slate-950 [&::-webkit-scrollbar]:hidden">
		{mesh ? (
		<div class="w-full relative">
			<div class="mesh-bg absolute inset-0 -z-10 hidden sm:block"></div>
			<div class="meshmobile absolute inset-0 -z-10 block sm:hidden"></div>
			<Header client:load/>
				<slot />
		</div>
		) : (
			<Header client:load/>
				<slot />
		)}
		<Footer client:load/>
	</body>
</html>